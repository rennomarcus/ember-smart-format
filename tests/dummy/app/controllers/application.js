import Controller from '@ember/controller';

export default Controller.extend({
  unformattedText: 'The quick fox **jumps** over the lazy dog\n' +
                   '[code javascript] Hello World [/code]\n'
});
