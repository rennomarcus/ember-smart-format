import limitLength from 'dummy/utils/limit-length';
import { module, test } from 'qunit';

module('Unit | Utility | limit length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = limitLength();
  assert.ok(result);
});
